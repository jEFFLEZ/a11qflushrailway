[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["NPM_CONFIG_PRODUCTION=false npm ci --include=dev"]

[phases.build]
cmds = ["NPM_CONFIG_PRODUCTION=false npm run railway:build"]

[start]
cmd = "npm run railway:start"
