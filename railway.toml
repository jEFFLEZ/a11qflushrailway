[build]
builder = "NIXPACKS"
buildCommand = "tsc -p . --outDir dist && node ./tools/copy-services-proxy.mjs && echo daemon-built"

[deploy]
startCommand = "node dist/daemon/qflushd.js"
healthcheckPath = "/health"
