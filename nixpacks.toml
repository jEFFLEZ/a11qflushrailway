[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["..."]

[phases.install]
cmds = ["env -u NPM_CONFIG_PRODUCTION npm ci --include=dev"]

[phases.build]
cmds = ["env -u NPM_CONFIG_PRODUCTION npm run railway:build"]

[start]
cmd = "node dist/daemon/qflushd.js"
